#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../sassdoc/bin/sassdoc" "$@"
  ret=$?
else 
  node  "$basedir/../sassdoc/bin/sassdoc" "$@"
  ret=$?
fi
exit $ret
